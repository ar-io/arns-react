name: Build and Test

on:
  push:
    branches: [ GH-actions-build-and-test ]


jobs:
  test_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm test
      - run: npm run build
