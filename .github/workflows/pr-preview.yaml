name: PR Preview Deployment

on:
  workflow_dispatch:
    inputs:
      undername:
        description: "Undername to deploy to (e.g., 'preview' for preview.arns.app)"
        required: false
        type: string
  pull_request:
    types: [opened, synchronize, reopened, closed]

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    # Run for: workflow_dispatch OR (PR from same repo that isn't closed)
    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.head.repo.full_name == github.repository && github.event.action != 'closed')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build application
        run: yarn build

      # Deploy to specific undername (workflow_dispatch with undername input)
      - name: Deploy to undername
        if: github.event_name == 'workflow_dispatch' && inputs.undername != ''
        uses: permaweb/permaweb-deploy@v0.0.2
        with:
          deploy-key: ${{ secrets.DEPLOY_KEY }}
          arns-name: ${{ secrets.ARNS_NAME }}
          undername: ${{ inputs.undername }}
          deploy-folder: ./dist

      # Deploy PR preview (pull_request or workflow_dispatch without undername)
      - name: Deploy PR preview
        if: github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && inputs.undername == '')
        uses: permaweb/permaweb-deploy@v0.0.2
        with:
          deploy-key: ${{ secrets.DEPLOY_KEY }}
          arns-name: ${{ secrets.ARNS_NAME }}
          preview: "true"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-folder: ./dist
